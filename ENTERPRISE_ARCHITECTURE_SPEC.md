# نسخة المؤسسات (Enterprise) — مواصفة الهيكلة والصلاحيات

> مصدر: تكليف من خطاب الجعيلي عبر تيليجرام، 2026-09-22. هذا المستند هو مرجع التصميم الرسمي لإعادة هيكلة نظام الصلاحيات متعدد الفروع. يجب مراجعته بالكامل قبل أي تعديل في الصلاحيات أو الـ Backend.

## أولاً: هيكل النسخ الثلاث

النظام يحتوي على ثلاث نسخ، ويجب التعامل مع كل نسخة حسب طبيعتها:

### النسخة الأولى: محل واحد + مخزن واحد
نشاط بسيط: محل واحد ومخزن واحد. مدير النشاط يمكن أن يملك كل الصلاحيات (إدارية وتشغيلية)، لأنه لا حاجة لفصل الإدارة المركزية عن الفروع.

### النسخة الثانية: محل واحد + مخازن متعددة
محل واحد، لكن أكثر من مخزن. مدير النشاط أيضاً يمكن أن يملك كل الصلاحيات، مع إمكانية إدارة المخازن والصلاحيات حسب الحاجة.

لا نحتاج في هاتين النسختين إلى تعقيد نسخة المؤسسات، لأن النشاط لا يحتوي على عدة فروع مستقلة.

### النسخة الثالثة: نسخة المؤسسات Enterprise
مخصصة للأنشطة متعددة الفروع بعدد كبير من الموظفين. يجب الفصل الواضح بين:
- **مدير النشاط** → الإدارة المركزية والإشراف
- **مدير الفرع** → إدارة وتشغيل الفرع
- **الموظف** → تنفيذ العمليات المسموح بها فقط

## ثانياً: مدير النشاط / مدير النظام

مسؤول عن إدارة المؤسسة بالكامل، وليس موظف تشغيل في فرع.

**إدارة المؤسسة:**
- إعدادات النشاط التجاري والإعدادات الأولية.
- إدارة الفروع (إضافة/تعديل/إيقاف).
- إدارة المخازن وربطها بالفروع.
- إدارة المناطق والتصنيفات.
- إدارة الأصناف والمنتجات والبيانات الأساسية.
- إدارة المستخدمين ومجموعات المستخدمين والصلاحيات.
- تعيين المستخدمين للفروع، وتعيين مدير/مشرف لكل فرع.
- إدارة بيانات الموظفين المرتبطة بالنظام.

**الإدارة المالية المركزية** (من اختصاص مدير النشاط):
- الرواتب، السلفيات، الخصومات، التسويات المتعلقة بالموظفين، متابعة المستحقات، أي عمليات مالية مركزية.
- يجب ربط هذه العمليات بالموظف وفرعه، حتى يمكن معرفة تكلفة الموظفين على مستوى المؤسسة أو كل فرع.

## ثالثاً: ما لا يقوم به مدير النشاط

لا يقوم بالعمليات التشغيلية اليومية للفروع: فاتورة بيع، شراء نيابة عن الفرع، استلام يومي، مصروفات تشغيلية للفرع، عمليات المخزون اليومية، أو أي عملية يومية من عمل موظفي الفرع.

لكن يجب أن يستطيع **قراءة ومراجعة** نتائج هذه العمليات عبر التقارير والإحصائيات. مدير النشاط يراقب ويدير من أعلى، ولا ينفذ التشغيل اليومي بدلاً من الفروع.

يمكن توفير خزنة مستقلة لمدير النشاط لتسجيل مصروفاته/معاملاته الشخصية، منفصلة عن خزائن الفروع.

## رابعاً: مدير الفرع

مسؤول عن التشغيل اليومي لفرعه فقط.

**يستطيع (حسب الصلاحيات):** المبيعات، المشتريات، الاستلامات، المصروفات، المخزون، إدارة مخازن الفرع، متابعة موظفي وعمليات الفرع، تقارير الفرع، العمليات التشغيلية الخاصة بالفرع.

**لا يستطيع:** إدارة فرع آخر، تغيير إعدادات المؤسسة، إدارة كل مستخدمي المؤسسة، إدارة الصلاحيات المركزية، أو الاطلاع على بيانات فروع أخرى إلا بصلاحية خاصة من مدير النشاط.

## خامساً: موظفو الفريق

المستخدم العادي لا يحصل تلقائياً على كل صلاحيات الفرع. تُحدَّد صلاحياته عبر السلسلة:

`المستخدم → المجموعة → الصلاحيات → الفرع`

مثال: موظف مبيعات ← صلاحية مبيعات فقط. موظف مخزن ← استلام ومخزون. موظف مشتريات ← مشتريات فقط.

يجب ربط المستخدم بفرع محدد بحيث لا يستطيع الوصول لبيانات فرع آخر ولو عرف الرابط أو حاول الوصول للـ API مباشرة.

## سادساً: الرواتب والسلفيات والخصومات

جزء من الإدارة المركزية في نسخة المؤسسات. مدير النشاط يستطيع: تعريف الراتب، تسجيل الرواتب/السلفيات/الخصومات، التسويات، متابعة المستحقات، معرفة إجمالي الرواتب وتكلفتها لكل فرع، بيانات موظف محدد، واستخراج التقارير.

يجب ربط كل موظف بفرعه لعرض: إجمالي رواتب المؤسسة / رواتب فرع محدد / بيانات موظف محدد.

## سابعاً: Dashboard والتقارير

Dashboard مدير النشاط على مستوى المؤسسة كاملة (الافتراضي = إجمالي كل الفروع)، مع فلتر موحد لـ: كل الفروع / فرع محدد / فترة زمنية / مخزن محدد عند الحاجة. الفلتر يعمل على كل التقارير المنطقية: المبيعات، المشتريات، المصروفات، الأرباح، المخزون، الرواتب... إلخ.

## ثامناً: عمليات تحتاج تفكير مركزي

نقل المخزون بين الفروع، تحويل المخزون بين المخازن، الأصناف/المنتجات المشتركة، الأسعار المركزية، التصنيفات، المستخدمين، الموظفين، الرواتب، السلفيات، الخصومات، التقارير الموحدة، الحسابات والإجماليات، الإعدادات العامة.

يجب تحديد لكل عملية: هل هي مركزية أم خاصة بالفرع، قبل تنفيذ الصلاحيات.

## تاسعاً: قاعدة البيانات — كل سجل مرتبط بالسياق الصحيح

- المبيعات → الفرع + المستخدم
- المشتريات → الفرع/المخزن + المستخدم
- المصروفات → الفرع + المستخدم
- المخزون → المخزن + الفرع
- الموظف → الفرع
- الراتب → الموظف + الفرع

بحيث عند طلب تقرير فرع معين، يستخرج النظام بياناته بدقة (وليس فقط إخفاء من الواجهة).

## عاشراً: الصلاحيات تُطبَّق في الـ Backend

ليس مجرد تغيير Sidebar. يجب مراجعة النظام بالكامل والتأكد من تطبيق الصلاحيات على:
Frontend, Views, APIs, Queries, Models/Business Logic, عمليات الإنشاء/التعديل/الحذف, التقارير, البيانات المُرجعة من الـ API.

مثال: لا يكفي إخفاء "المبيعات" من قائمة مدير النشاط؛ يجب أيضاً منعه من الوصول لـ API إنشاء فاتورة مباشرة.

## الحادي عشر: العزل بين النسخ الثلاث (مهم جداً)

أي تعديل بسبب نظام المؤسسات **يجب ألا يغيّر سلوك النسخة الأولى أو الثانية**. يجب تحديد نوع النسخة بوضوح:
- Single Store
- Multi-Store
- Enterprise / Multi-Branch

ثم تطبيق منطق الصلاحيات المناسب لكل نسخة. النسخة الأولى والثانية تحتفظ بالصلاحيات الواسعة الحالية لمدير النشاط. نسخة المؤسسات فقط تستخدم الفصل: مدير النشاط → الفروع → المستخدمين → الصلاحيات.

## الثاني عشر: خطة الاختبار (بعد التنفيذ)

مراجعة شاملة، وليس فقط اختبار الصفحات المعدَّلة. سيناريوهات لازم اختبارها على الأقل:
1. إنشاء مؤسسة جديدة.
2. إنشاء عدة فروع.
3. إنشاء مخازن وربطها بالفروع.
4. إنشاء مستخدمين.
5. توزيع المستخدمين على الفروع.
6. إنشاء مدير لكل فرع.
7. إنشاء مجموعات وصلاحيات.
8. التأكد أن المستخدم لا يرى بيانات فرع آخر.
9. التأكد أن مدير الفرع لا يستطيع إدارة المؤسسة.
10. التأكد أن مدير النشاط يرى كل الفروع.
11. اختبار Dashboard الإجمالي.
12. اختبار Dashboard لفرع محدد.
13. اختبار كل التقارير مع فلتر الفرع.
14. اختبار المبيعات والمشتريات والمصروفات.
15. اختبار المخزون.
16. اختبار الرواتب.
17. اختبار السلفيات.
18. اختبار الخصومات.
19. اختبار العمليات بين الفروع والمخازن إن كانت متاحة.
20. التأكد من عدم تأثر النسخة الأولى والثانية بأي تعديل.

## النتيجة المطلوبة

- مدير النشاط يرى المؤسسة كلها ويديرها مركزياً.
- مدير الفرع يدير فرعه ويشرف على فريقه.
- الموظف ينفذ العمليات المسموح بها فقط.
- النسخة الأولى والثانية تظل بسيطة ومرنة كما هي.

أي تعديل يجب أن يتم بعد مراجعة كل الأجزاء المرتبطة، حتى لا يتم إصلاح الصلاحيات في مكان وتظهر مشكلة في API أو التقارير أو المخزون أو المستخدمين في مكان آخر.
